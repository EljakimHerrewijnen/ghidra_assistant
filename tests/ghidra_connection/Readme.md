# Ghidra Connection Example

Connect to a running Ghidra instance and list structures through the GA API.

This folder also contains pytest examples for Ghidra connection/coloring flows.

Run from repository root:

```bash
pdm run python tests/ghidra_connection/structs.py <path-to-target-binary>
```